$kind: http-request
name: Delete Artist (Admin)
method: DELETE
url: '{{baseUrl}}/api/v1/artists/{{artistId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
